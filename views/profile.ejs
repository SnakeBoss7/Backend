<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

</head>

<body>
    <div class="bg-teal-600 w-full h-full p-4 flex flex-col">
        <div class="flex w-full justify-end">
            <a href="/logout" class="bg-black w-20 text-pink-900 px-2 m-5 py-1 rounded-2xl inline-block ">Logout</a>

        </div>
        <div class="w-20 h-20 rounded-lg">
              <img class="w-full h-full object-cover" src="/images/uploads/<%=user.profilepic%>" alt="">  
        </div>
        <h1 >Welcome <%=user.name%></h1>
        <h5 class="">You can create a new post</h5>
        <form action="/post" method="post" class="flex flex-col">
            <textarea class="border-gray-300 p-3 bg-3ray-200" placeholder="write here" name="content"></textarea>
            <input class="mt-3 bg-yellow-500 w-content  rounded-md "  type="submit">
        </form>
        <div class="posts mt-10">
            <h3 class="text-gray-800 ml-5">Your posts</h3>
            <div class="postcontainer text-white  ">
                <%user.posts.reverse().forEach(function(post){%>
                <div class="post bg-slate-800 shadow-2xl text-white-400 rounded-md mx-3  p-3 my-3">
                    <h4 class="">@<%=user.username%></h4>
                    <p class=""><%=post.content%></p>
                    <small class="mt-2 inline-block"><%=post.likes.length%> Likes</small>
                    <div class="btns">
                        <a href="/like/<%=post._id%>">
                            <%= post.likes.indexOf(user._id)=== -1?"👍🏻":"👎🏻"%>
                        </a>
                        <a href="/edit/<%=post._id%>" class="mx-2">✏️</a>
                    </div>
                </div>
                    
                <% })%>
                
                 

                

            </div>
        </div>
    </div>
</body>

</html>